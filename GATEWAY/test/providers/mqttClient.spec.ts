import { MqttClient } from '../../src/providers/mqttClient';

describe('MqttClient', () => {
  /*
    beforeEach( () => {
      //this.mqttclient = new MqttClient();
    });
    xit('should return a string that is an url to the specific device running the getDeviceSpecificTopic() method. This test does NOT involve an event trigger', () => {
      const deviceID: String = "trollydrolly";
      // TODO: change the testUsername in the last argument of assert.equal to a propper test username from the mqttclient
      expect(this.mqttclient.getDeviceSpecificTopic(deviceID, false)).toEqual('tiles/false/testUsername/' + deviceID);
    });
    
    xit('should return a string that is an url to the specific device when running the getDeviceSpecificTopic() method. This test DOES involve an event trigger', () => {
      const deviceID: String = "trollydrolly";
      // TODO: change the testUsername in the last argument of assert.equal to a propper test username from the mqttclient
      // TODO: Make sure that an event is called with the test AKA send to the correct device.
      expect(this.mqttclient.getDeviceSpecificTopic(deviceID, true)).toEqual('tiles/true/testUsername/' + deviceID);
    });

    xit('should return false when we change the connection status to the server with setServerConnectionStatus() ', () => {
      const falseConnection = false;
      expect(this.mqttclient.setServerConnectionStatus(falseConnection)).toEqual(false);
    });

    xit('should return true when we change the connection status to the server with setServerConnectionStatus()', () => {
      const trueConnection = true;
      expect(this.mqttclient.setServerConnectionStatus(trueConnection)).toEqual(true);
    });
*/
});
