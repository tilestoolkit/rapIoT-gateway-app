import { DevicesService, Device } from '../../src/providers/devices.service';
import * as TilesDevice from '../fixtures/tilesDevice.json';
import * as BleDevice from '../fixtures/bleDevice.json';


describe('DevicesService', () => {
  /*
  xit('should have an empty list "devices" after construction', () => {
    const devices = new DevicesService();
    expect(devices.getDevices().length).toEqual(0);
  });

  xit('should return a list with three mock devices when running `getMockDevices()`', () => {
    const devices = this.devicesService.getMockDevices();
    expect(devices.length).toEqual(3);
  });

  xit('should have one more device in "devices" after running newDevice(device)', () => {
    const device: Device = TilesDevice;
    this.devicesService.newDevice(device);
    expect(this.devicesService.getDevices().length).toEqual(1);
  });

  xit('should return true since the device is not allready in the devices list', () => {
    const device: Device = TilesDevice;
    expect(this.devicesService.isNewDevice(device)).toEqual(true);
  });

  xit('should return false since the device is allready in the devices list', () => {
    const firstDevice: Device = TilesDevice;
    this.devicesService.newDevice(firstDevice);
    const secondDevice: Device = TilesDevice;
    expect(this.devicesService.isNewDevice(secondDevice)).toEqual(false);
  });
  */
});
