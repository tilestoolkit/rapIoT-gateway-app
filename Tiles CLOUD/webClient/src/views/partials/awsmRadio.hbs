<div class="radio radio-{{type}}">
    <input type="radio" name="{{name}}" id="{{id}}" value="{{value}}" {{#if isChecked}}checked{{/if}}>
    <label for="{{id}}">
        {{label}}
    </label>
</div>